# OS
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*.swo
*~

# IDE
.idea/
.vscode/
*.code-workspace
.cursor/

# Node
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Environment
.env
.env.*
!.env.example

# Credentials (NEVER commit)
CREDENTIALS.md
credentials/
*.credentials

# Python
__pycache__/
*.py[cod]
*.egg-info/
venv/
.venv/

# Flutter / Dart — keep web build for deployment
mobile/build/*
!mobile/build/web/
mobile/.dart_tool/
mobile/.packages
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
# mobile/pubspec.lock — committed for reproducible builds

# Supabase
supabase/.temp/

# Vercel — committed for deployment config
# .vercel/

# Logs
logs/
*.log

# Build artifacts — web builds committed for deployment
# dist/
# build/
.cache/

# Backups & exports
*.tar.gz
*.zip
exports/
backups/

# -------------------------------------------------------
# SECURITY: credentials and secrets — NEVER commit these
# -------------------------------------------------------
# Private keys and certificates
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
*.cer
*.crt
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub

# Firebase / Google service accounts (admin-level only)
# NOTE: google-services.json & GoogleService-Info.plist are KEPT in repo
# (Firebase client API keys are restricted by app signing, safe to commit)
*firebase-adminsdk*.json
service-account.json
google-credentials.json

# Files that commonly contain secrets
*secrets*.json
*secret*.env
.env.production
.env.staging
.env.local

# git-secrets config (keep out of repo)
.git-secrets
